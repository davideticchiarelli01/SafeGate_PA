services:
  app:
    container_name: safegate_app
    build: .
    ports:
      - '${APP_PORT}:${APP_PORT}'
    restart: always # when the docker engine is started this option allow starting the container too
    env_file:
      - .env
    volumes:
      - ./jwt_keys:/usr/src/app/jwt_keys:ro # Read-only volume for JWT keys
      - ./src/logger/logs:/usr/src/app/src/logger/logs
    depends_on:
      postgres:
        condition: service_healthy
    command: >
      sh -c "npm start"

  postgres:
    container_name: postgres_db
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT}:${DB_PORT}"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./src/db/init:/docker-entrypoint-initdb.d:ro # Read-only volume for initialization scripts
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER}" ]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
