/**
 * Enumeration representing the roles a user can have in the system.
 * Used to control access and permissions for different types of users.
 *
 */
export enum UserRole {
    Admin = 'admin',
    User = 'user',
    Gate = 'gate',
}