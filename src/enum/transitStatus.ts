/**
 * Enumeration representing the possible outcomes of a transit.
 * Indicates whether a badge transit was allowed or denied.
 *
 */
export enum TransitStatus {
    Authorized = "authorized",
    Unauthorized = "unauthorized"
}