/**
 * Enumeration representing the types of required DPIs.
 * Used to define the DPIs that a gate may require and a badge may use during a transit.
 *
 */
export enum DPI {
    Helmet = 'helmet',
    Gloves = 'gloves',
    Vest = 'vest',
}
